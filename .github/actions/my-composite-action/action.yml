name: "My Composite Action"
description: "A reusable composite action"
inputs:
  version:
    description: "Version of the environment"
    required: true
    default: "latest"

runs:
  using: "composite"
  steps:
    - name: Print version
      run: echo "Using version ${{ inputs.version }}"